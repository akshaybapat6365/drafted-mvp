rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through privileged backend functions.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
