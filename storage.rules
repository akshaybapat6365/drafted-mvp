rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Artifacts are served through signed URLs issued by backend.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
